import React from 'react';

function ButtonBucketAdd(props) {
   
    return (
        <button className="btn card_btn bucketlist-add" {...props}>Add to Bucket List</button>
    );
};

export default ButtonBucketAdd;
 
 