import React from 'react';

function ButtonLogAdd(props) {
 
    return (
        <button className="btn card_btn bucketlist-add" {...props}>Add to Log</button>
    );
};

export default ButtonLogAdd;
 
 